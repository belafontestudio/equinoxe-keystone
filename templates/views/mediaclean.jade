extends ../layouts/default

block content
    
    //- h1= getCatalog()
    //- h1= setLocale('it')
    //- h1= __('hello')
    //- h1= getLocale()
    
    block menu 
      include ../partials/_menu.jade
    block body
      #onepage

    //- if user && user.canAccessKeystone
    //-     p
    //-         a(href='/keystone/signin').btn.btn-lg.btn-primary Open the Admin UI
    //- else
    //-     p We have created a default Admin user for you with the email <strong>piermaria@belafonte.co</strong> and the password <strong>piermaria</strong>.
    //-     p
    //-         a(href='/keystone/signin', style='margin-right: 10px').btn.btn-lg.btn-primary Sign in
    //-         |  to use the Admin UI.
        
